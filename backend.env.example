# Backend Environment Variables for Render
# Copy these to Render Dashboard -> Environment Variables

NODE_ENV=production
PORT=5000

# MongoDB Connection (replace with your actual connection string)
MONGO_URI=mongodb+srv://timesheet_admin:YOUR_PASSWORD@timesheet-db.xxxxx.mongodb.net/timesheet?retryWrites=true&w=majority

# JWT Configuration (generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long-replace-this
JWT_EXPIRE=7d

# Frontend URL (update after deploying frontend)
FRONTEND_URL=https://timesheet-frontend.onrender.com

# Optional: Email configuration (for Phase 1.5)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
